{{ config(
    materialized='table'
) }}

SELECT

    ------------------------------------------------------------------
    -- Session Metrics
    ------------------------------------------------------------------

    session_duration_sec,

    total_events,

    total_engagement_time,

    ------------------------------------------------------------------
    -- Behaviour Features
    ------------------------------------------------------------------

    pageviews,

    unique_pages,

    item_views,

    searches,

    add_to_cart,

    begin_checkout,

    add_shipping_info,

    add_payment_info,

    ------------------------------------------------------------------
    -- Device
    ------------------------------------------------------------------

    device_category,

    operating_system,

    ------------------------------------------------------------------
    -- Geography
    ------------------------------------------------------------------

    country,

    ------------------------------------------------------------------
    -- Acquisition
    ------------------------------------------------------------------

    session_source,

    session_medium,

    session_campaign,

    ------------------------------------------------------------------
    -- Label
    ------------------------------------------------------------------

    purchased

FROM {{ ref('mart_checkout_abandonment') }}